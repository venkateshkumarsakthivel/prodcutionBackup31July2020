@isTest
public class UpdateAllegedBreachTimeSchedulerTest {
    
    @isTest
    private static void executeTest() {        
        Test.startTest();
        new UpdateAllegedBreachTimeScheduler().execute(null);
        Test.stopTest();
    }

}